Money Map v2.0.3.27 — Production Web

- Applies the approved grouped Settings design while preserving the profile cover, photo, name and membership details.
- Keeps Notification center and Recurring transactions as independent options in the three-dot menu.
- Keeps Security and Data as single Settings destinations that open their existing detail windows.
- Groups all notification controls and adds Budget alerts at 95% plus a 90-day backup reminder.
- Shows a green tick inside enabled toggles and a grey x inside disabled toggles.
- Budget alerts fire once per category per month when a saved transaction crosses the threshold.
- Backup reminders are checked at app launch/resume and reset after each successful backup.
- Existing Android closed-app schedules remain unchanged; no APK update is included.
- Financial calculations, stored records, profile, PIN/fingerprint, backup/restore, recurring transactions, assets/history and the approved logo are preserved.
