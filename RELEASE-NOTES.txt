Money Map v2.0.3.61 - upload these two files only

Money_Map.html -> replace in money-map-main (repo root)
sw.js          -> replace in money-map-main (repo root)

WHAT CHANGED
1. Photo card (overview + settings) rebuilt from the app own popup parts.
2. Back closes the card first, then the big picture, then the screen.
3. Camera and gallery now open above the edit screen, crop area visible.
4. Cropping is a square box, WhatsApp style, not a circle.
5. Financial health: point-less duplicate rows gone; year picker is the app picker.
6. Back button rebuilt: one spare entry, retraces real screens, exits at overview.

Service worker cache: moneymap-v20361-core (forces a refresh on the phone).
