Money Map v2.0.3.25 — Production

- Replaces the WebView fade with a native theme-matched startup cover that hides all unpainted WebView frames.
- Uses transparent/no-preview Android starting windows so the system cannot insert an opposite-colour screen.
- Reveals the ready app by fading the cover for 220ms; fallback reduced to 350ms after page finish.
- Consolidates page, welcome, lock, setup, modal, navigation, card and progress motion into one faster native-style curve.
- Removes exaggerated navigation bounce and all stagger delays.
- Retains X Dim dark mode, approved icon, contained welcome blur, ring thickness and notification logic.
- Financial calculations and stored data are unchanged.
